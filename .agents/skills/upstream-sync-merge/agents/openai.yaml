interface:
  display_name: "Upstream Sync Merge"
  short_description: "Merge upstream code and preserve local integration"
  default_prompt: "Use the repository upstream-sync-merge workflow to merge upstream/main, preserve local-owned files, verify the result, and stop on real conflicts or LFS blockers."
