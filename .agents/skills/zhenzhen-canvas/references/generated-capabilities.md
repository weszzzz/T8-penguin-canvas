# Generated creative capabilities

> Machine-generated from `tools/zcanvas-cli/creativeCapabilityManifest.json`.
> Do not edit this file by hand. It is an index only and never overrides the Skill safety contract.

- Source schema: `t8-creative-capability-manifest-v1`
- Source version: `0.1.0-dev`
- Source SHA-256: `5c56b3b64d483ae6e6da5268f25c059f2a3fcc2c13e3c3d64d39b10e1e56402b`
- Registered capabilities: **31**

All write or generation paths shown here remain subject to the human-readable Skill rules:
inspect → plan → preview → explicit approval → apply/run → verify.

## image

- `create.image` — **生成图片** (L2): 从一句要求创建可编辑的图片候选工作流。
  - handler: `creative-plan:create.image`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`
- `image.remove-solid-background` — **移除纯色背景** (L1): 建立本地边缘连通纯色背景移除节点；它不是通用 AI 抠图，不会冒充复杂背景分割。
  - handler: `creative-action:image.remove-solid-background`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`, `rollback`
- `image.resample-upscale` — **Lanczos 等比放大** (L1): 建立本地 Lanczos3 等比重采样节点；它只增加像素尺寸，不宣称 AI 超分或细节修复。
  - handler: `creative-action:image.resample-upscale`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`, `rollback`
- `edit.image` — **修改图片** (L2): 基于已持久化图片建立不覆盖原结果的编辑分支。
  - handler: `creative-plan:create.edit-image`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`, `rollback`

## video

- `create.video` — **生成视频** (L2): 创建可编辑的视频候选工作流并保留素材连续性。
  - handler: `creative-plan:create.video`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`
- `video.extract-frames` — **视频抽帧** (L1): 为一个兼容视频上游建立浏览器本地均匀抽帧节点；应用只创建可编辑工作流，运行需单独批准。
  - handler: `creative-action:video.extract-frames`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`, `rollback`
- `edit.video` — **修改视频** (L2): 基于已持久化视频建立不覆盖原结果的编辑分支。
  - handler: `creative-plan:create.edit-video`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`, `rollback`

## audio

- `create.audio` — **生成音频** (L2): 创建配音、音乐、声音或转写工作流。
  - handler: `creative-plan:create.audio`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`

## script

- `create.script` — **完善剧本** (L2): 把一句创意整理为可继续编辑的剧本工作流。
  - handler: `creative-plan:create.script`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`

## production

- `create.story` — **全自动制片** (L2): 创建从剧本、资产、分镜、提示词到视频与成片的 Story 制片对象。
  - handler: `creative-plan:create.story`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`, `resume`
- `story.analyze` — **分析剧本** (L2): 分析 Story 的场景、镜头、资产与声音需求，不自动开始生成。
  - handler: `creative-action:story.analyze`
  - supports: `plan`, `preview`, `apply`, `verify`
- `story.import` — **导入 Story** (L2): 把结构化 Story 制片对象安全导入当前画布。
  - handler: `creative-action:story.import`
  - supports: `plan`, `preview`, `apply`, `verify`
- `story.bind-asset` — **绑定资产** (L2): 把已持久化素材绑定到 Story，同时保留来源和版本。
  - handler: `creative-action:story.bind-asset`
  - supports: `plan`, `preview`, `apply`, `verify`, `rollback`
- `story.compile` — **合成提示词** (L2): 根据已确认镜头和资产编译可编辑的视频提示词。
  - handler: `creative-action:story.compile`
  - supports: `plan`, `preview`, `apply`, `verify`
- `story.plan-previews` — **计划分镜预览** (L2): 只为缺失、失败且未锁定的 Story 项目建立预览计划。
  - handler: `creative-action:story.plan-previews`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`, `resume`
- `story.adopt-preview` — **采用预览** (L2): 采用一个已持久化候选并保留其他版本。
  - handler: `creative-action:story.adopt-preview`
  - supports: `plan`, `preview`, `apply`, `verify`, `rollback`
- `director.materialize` — **生成导演分镜台** (L2): 从 Story 物化导演分镜台并绑定同一生产状态。
  - handler: `creative-action:director.materialize`
  - supports: `plan`, `preview`, `apply`, `verify`

## canvas

- `canvas.node-add` — **新增画布节点** (L1): 依据权威节点 Schema 新增一个可编辑节点；只创建画布结构，不自动运行模型或外部任务。
  - handler: `creative-action:graph.node-add`
  - supports: `plan`, `preview`, `apply`, `verify`, `rollback`

## delivery

- `video-edit.compose` — **合成为片** (L2): 把已完成镜头物化到剪辑台，保留镜头顺序和素材来源。
  - handler: `creative-action:video-edit.compose`
  - supports: `plan`, `preview`, `apply`, `run`, `verify`, `resume`
- `delivery.package` — **打包交付** (L2): 收集、打包并复核图片、视频、音频和文本交付物。
  - handler: `delivery:package`
  - supports: `plan`, `preview`, `apply`, `verify`

## review

- `iterate.compare` — **比较候选** (L0): 读取候选版本、锁定项与真实媒体证据，不修改画布。
  - handler: `creative-read:iterate.compare`
  - supports: `inspect`, `verify`
- `iterate.review` — **检查实际候选** (L2): 保存基于真实图像、视频、音频或文本作品的结构化检查证据。禁止只按 Prompt 或模型名评价。
  - handler: `creative-action:review`
  - supports: `plan`, `preview`, `apply`, `verify`
- `iterate.accept` — **采用候选** (L2): 仅在真实作品评审完整且硬性检查通过后采用候选，并保留其余版本供比较。
  - handler: `creative-action:accept`
  - supports: `inspect`, `plan`, `preview`, `apply`, `verify`, `rollback`
- `iterate.lock` — **锁定一致性** (L2): 锁定已确认的角色、产品、风格、提示词或参数，后续只改未锁定部分。
  - handler: `creative-action:lock`
  - supports: `plan`, `preview`, `apply`, `verify`, `resume`
- `iterate.unlock` — **解除一致性锁** (L2): 在明确确认后解除候选的一致性锁，使后续迭代可以修改对应内容。
  - handler: `creative-action:unlock`
  - supports: `plan`, `preview`, `apply`, `verify`, `rollback`
- `iterate.branch` — **创建探索分支** (L2): 保留当前候选，从它创建独立可迭代分支，不覆盖已接受版本。
  - handler: `creative-action:branch`
  - supports: `plan`, `preview`, `apply`, `compare`, `verify`, `rollback`
- `iterate.rollback` — **恢复候选版本** (L2): 恢复候选的已保存版本，同时保留当前状态作为可追溯版本。
  - handler: `creative-action:rollback`
  - supports: `plan`, `preview`, `apply`, `verify`, `rollback`

## execution

- `run.start` — **开始生成** (L2): 在明确批准后启动已预览的运行计划。
  - handler: `run:start`
  - supports: `preview`, `run`, `verify`, `resume`, `cancel`

## asset

- `asset.place` — **把已保存素材放到画布** (L1): 把当前项目中已校验的素材作为可撤回节点放入画布，可选连接到明确节点和端口。
  - handler: `asset:place`
  - supports: `plan`, `preview`, `apply`, `verify`, `rollback`
- `asset.import` — **导入外部素材** (L2): 在明确批准后把本机文件导入当前项目素材中心。
  - handler: `asset:import`
  - supports: `preview`, `apply`, `verify`

## handoff

- `browser.handoff` — **浏览器接力** (L1): 仅在用户明确要求时，为当前本机画布创建可见、无登录态继承的受限 handoff。
  - handler: `browser:open`
  - supports: `preview`, `apply`, `verify`
