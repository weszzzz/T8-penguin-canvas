# T8 Penguin Canvas v2.9.6

本版修正 v2.9.5 视频超分节点的公开名称：节点、模型目录、默认值与无密钥工作流统一使用 `FlashVSR 视频超分` / `FlashVSR_video_upscale`。默认渠道、Seedance 2.5 `native1080p`、MiniMax Music3、旧画布及全部既有功能保持不变。

## 修正

- 当前模型只展示并提交 `FlashVSR_video_upscale`，不再把旧拼写显示为可选模型。
- v2.9.5 已保存画布中的 `FashVSR_video_upscale` 仍可正常载入；请求层会在付费提交前规范化为新名称。
- 稳定节点类型 `fashvsr-video-upscale`、恢复 kind、代理路由和任务身份保持不变，不迁移、不复制节点，也不使进行中任务失效。
- 所有用户可见校验、提交、轮询、失败与下载提示同步为 FlashVSR。

## 真实验证

- 新拼写使用贞贞的平价 AI 小屋真实提交一次且只提交一次，任务成功完成。
- 输入：854×480、H.264、3.208008 秒、472,060 bytes。
- 输出：1920×1024、H.264、3.208333 秒、1,004,338 bytes；SHA-256 `5cb14e96c5002c3a417b11acdae159ae235dee8e46ae7d7f8574d129c2778472`。
- 输出下载、FFprobe 和全量解码通过；验证报告不含密钥。

## 完整保留

- MiniMax Music3 提示词增强器已随 v2.9.3 正式发布。本版继续完整包含其节点、工作流、官方固定 Skill 快照、1,022 个上游文件、18 个风格族索引、1,000 个模板及来源/清单文件。
- v2.9.5 Seedance 2.5 六模型 `native1080p`、v2.9.4 LLM 与 Issues 修复、v2.9.2 启动优化，以及全部既有节点、默认渠道、默认模型和历史工作流均不回退。

## 正式发布

- 固定源码：`dce1adf081893f290b65c3be1f14b0f150e46a1f`；Tag 与 Release target 精确一致。
- 发布时间：`2026-08-18T14:21:09Z`；Release 非草稿、非预发布，并为 GitHub Latest。
- 安装包：`T8-PenguinCanvas-Setup-2.9.6.exe`，1,354,114,810 bytes，SHA-256 `bc2670b47d3065e4be6a95a01ff0063b491ed7481897ffd6276ac3e882d58c04`。
- blockmap：1,413,270 bytes，SHA-256 `ee1647565e22aa9cc68479908e9a84054e3a305f5042f410afd9a6cc8d9f8b49`；`latest.yml`：362 bytes，SHA-256 `03b998ab76ee7fbe4eee649567522f269ccb974b283c098117605cefaf070c6b`。
- 正式流程完成私有构建、后端与本地私有文件加密、两套运行时归档验证、Electron native rebuild、NSIS、post-build、provenance、sealed recovery、发布前三资产完整回下载及发布后 Latest/digest/size 核验；本地恢复记录已清除。

## 证据边界

官方 `llms.txt` 在本次核对时仍显示旧拼写，因此不声称文档页面已经同步；新拼写以实际成功 Provider 任务为运行证据。用户授权先发布自动更新包、后补反馈用户安装、多设备协作和安装升级证据，精确令牌为 `owner-approved-post-release-v2.9.6`。该延期不等于通过；所有技术构建、摘要、固定 Tag/target、资产完整回下载和 Latest 门禁仍必须通过。
