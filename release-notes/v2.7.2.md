# T8 Penguin Canvas v2.7.2

本版集中修复 RunningHub AI 应用返回文本结果时画布无限轮询、任务已完成却迟迟不出结果的问题，并保持图像、视频、音频等既有媒体结果链路不变。

## RunningHub 文本结果终态

- RunningHub 查询现在会结合 `fileType`、MIME 和签名 URL 扩展名识别 TXT、Markdown、JSON 与 CSV，不再把文本产物送入图像/视频/音频魔数校验。
- 文本使用有界安全下载读取并统一持久化为本地 UTF-8 文件；纯文本和文本 + 媒体混合任务会在同一次 `SUCCESS` 响应返回。
- 普通 RunningHub 节点、RH AI 应用节点和 RH 工具箱统一接收 `texts` / `textUrls`，同步写入 `outputText`、`text`、`texts` 与 `textSegments`，可继续沿画布连线传给下游。
- 已完成的 taskId 只查询并转存原结果，不重新提交任务，不会造成重复生成或重复扣费。

## 真实验证

- 使用指定 WebApp `2082878851868348418` 完成真实调用；上游实际返回 TXT + FLAC。
- 以 3 秒间隔查询时仅经历 `RUNNING → SUCCESS`，没有进入 `MATERIALIZING`，最终得到 1 个本地 FLAC、1 段文本和 1 个本地 TXT。
- 再次查询同一 taskId 仍稳定返回相同文本与媒体地址，没有重新提交任务。
- RunningHub 文本、站点与 RH 工具箱定向回归 32/32 通过。

## 发布边界

- Electron/NSIS 正式完整打包只执行一次，并使用受限资源策略；不运行 FFmpeg 转码或额外压力任务。
- 用户已明确授权先发布、后补真实 Provider、协作多端与安装升级证据；延期不会把尚未执行的证据标记为通过。

## 自动更新

正式 Release：

- GitHub：<https://github.com/T8mars/T8-penguin-canvas/releases/tag/v2.7.2>
- Tag / Release target：`8ecc882fb6e030b38c7f37b4d16e69267cce537f`
- `T8-PenguinCanvas-Setup-2.7.2.exe`：1,304,665,337 bytes；SHA-256 `26847cd07c4dc1bbb491ec3ebd9688e3aa7e47d1bbefd91b43e60c50d0d2cf44`
- `T8-PenguinCanvas-Setup-2.7.2.exe.blockmap`：1,359,962 bytes；SHA-256 `d60ba173f3de3a748efb9cee36db8f63d6b73c81826f5a66e03cd8e50eebf9ac`
- `latest.yml`：362 bytes；SHA-256 `f2a76e9fb8ae5e52b5e1efae2c0067c67a2ad5c937f3f08fd3d593a663bed818`

三项资产已通过 post-build、sealed recovery、GitHub digest/size 元数据和发布前完整远端回下载字节核验。Release 非草稿、非预发布并已成为 Latest；发布后只追加本记录，不移动已固定的 v2.7.2 Tag。
