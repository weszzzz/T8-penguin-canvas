# T8 Penguin Canvas v2.9.3

本版在完整保留 v2.9.2 启动、目录、主题、媒体调度与跨画布后台运行优化的基础上，正式加入 MiniMax Music3 提示词增强器。既有节点、模型、默认渠道、用户画布、工作流与 Provider 协议均不回退。

## MiniMax Music3 提示词增强器

- 新增 `minimax-music3-prompt-enhancer` 纯文本节点，服务于音乐生成前的歌词、结构、曲风、编曲、演唱与 Caption 策划，不直接生成音频。
- 歌词支持 AUTO、生成、逐字保留、定域润色与纯音乐模式；可编辑全曲、同名全部段落或指定第 N 个段落。
- 支持结构预设、语言、拍号、目标时长、BPM、调性、Caption 字数、创作限制与随机种子。
- 输出最终歌词、Music 3 Caption、标准 Payload 与脱敏报告，可连接标准文本和元数据下游节点。

## 官方 Skill 与渠道

- 安装包内置 `MiniMax-AI/MiniMax-Music3` 固定提交 `91410fb657c007ae57c60df8240f5ece5be089c7` 的官方 Skill 快照：1,022 个文件、18 个风格族索引、1,000 个完整模板。
- 资源来源、提交和规范化树 SHA-256 `d836359b48a4bc3381f8d9eb370ff90dd82cb5ad9aa4e3ba0ed80da2c25b2553` 均有清单校验。项目所有者已明确授权把该固定快照纳入正式安装包；本项目不额外声称上游许可证条款。
- 节点按渐进披露读取参考：每次最多 2 个风格族索引和 3 个模板，不会把全部 1,000 个模板一次发送给 LLM。
- 默认渠道与模型保持为贞贞的平价AI小屋 `bytedance/doubao-seed-2.1-pro`，同时支持画布现有内置工坊和全部已启用扩展 LLM 渠道。

## 运行与安全边界

- 默认私有语义模式不会把原始歌词行发送到 Caption 阶段；疑似 API Key、不安全控制标签和超限输入会在提交前失败关闭。
- 多阶段请求使用持久 child attempt 与 submission ledger；不明确的 Provider 提交不会自动重放，避免重复计费。
- STOP、后台跨画布运行、节点实体身份、revision CAS 与旧画布持久化行为继续沿用 v2.9.2 安全边界。

## 正式发布与验证

- 官方资源清单已验证 1,022/1,022 文件、18 个索引、1,000 个模板与固定树哈希一致。
- Music3、端口、运行入口、RunIntent 与能力图聚焦回归 42/42 通过；生成能力目录为 31 actions / 250 runtime entries / 78/78 nodes / 0 public gaps。
- 固定 source commit `454a756e4b0f4788af76079aa0df3af6568da0a5` 已完成 TypeScript、2,888 模块私有构建、后端与 4 个本机私有文件加密、双运行时归档、Electron 原生模块重建、NSIS、post-build、provenance、sealed recovery、固定 Tag/target 与 GitHub Latest。
- 安装目录再次实测包含 1,000 个模板、18 个索引、1,022 个上游文件及 SOURCE/NOTICE/manifest。三项自动更新资产在发布前和独立发布后各完整回下载一次，并与本地字节、SHA-256、installer SHA-512、latest.yml 及 GitHub digest/size 一致。
- 安装包：1,317,061,320 bytes，SHA-256 `e739dead29c500517ca00fa53b5e3858b495a9bf136dd90e16ce4e35ee15f07a`。
- blockmap：1,371,959 bytes，SHA-256 `933c798fb7f60aae3ef323d5cac445efcee767a8e04ed3504467eb9bc9b157bc`；`latest.yml`：362 bytes，SHA-256 `5d3c1ba35d602592dc690f530356e813b538e545f17ecf3733e285d92bafa870`。
- 正式版本：<https://github.com/T8mars/T8-penguin-canvas/releases/tag/v2.9.3>，发布时间 `2026-08-15T04:32:51Z`，非草稿、非预发布并为 GitHub Latest。
- 用户已授权先发布、后补反馈用户安装环境、多人协作与安装升级证据；仅允许精确 `owner-approved-post-release-v2.9.3` 对完全缺失的证据清单延期，延期不等于通过，任何技术门禁失败都不可延期。
