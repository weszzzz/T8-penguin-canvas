# T8 Penguin Canvas v2.9.7

本版修复部分用户从 v2.9.1 更新到 v2.9.2–v2.9.6 后，画布项目在再次启动时可能出现“项目数据库 typed canonical JSON 不变量校验失败”的问题。修复保持启动只读、历史数据兼容范围精确，并继续拒绝非匹配的数据库损坏。

## 修复

- 根因是 v2.9.2 起的画布目录重命名路径更新了 SQL `canvas_documents.updated_at`，却只把 `name` 与 `nodeCount` 写回 `snapshot_json`，遗漏同步 JSON `updatedAt`。
- 今后的目录元数据写入使用同一个时间值同时更新 SQL 行和画布 JSON，确保冷启动 typed canonical 校验一致。
- v2.9.2–v2.9.6 已产生的旧漂移只在精确签名下兼容：SQL 时间严格晚于合法 JSON 时间、目录名合法非空，并且 `nodeCount` 精确等于 `nodes.length`。
- 兼容路径不会在启动时静默改写用户项目数据库；运行时继续以 SQL 行时间为权威值，下一次真实写入才自然保存 canonical JSON。
- 其他时间异常、节点数量不一致、schema/身份/revision 损坏仍按原安全策略失败关闭。

## 验证

- 新目录重命名会同时保存 JSON `updatedAt` 与 SQL `updated_at`，并可冷重开。
- 精确复现 v2.9.2–v2.9.6 历史漂移后可只读冷重开，且存储的 JSON 不会在启动期间被改写。
- 构造同类时间漂移但篡改 `nodeCount` 时仍触发 typed canonical 错误，证明兼容范围没有扩大为通用绕过。
- 测试数据库只在系统临时目录创建并于测试结束后清理；没有读取、复制或修改用户保留项目数据库。

## 完整保留

- v2.9.6 的 FlashVSR 名称修正、v2.9.5 Seedance 2.5 `native1080p`、v2.9.3 MiniMax Music3、全部 79 个节点、默认渠道、默认模型、旧画布与既有工作流均不回退。
- 正式安装包继续要求本机私有扩展、后端加密、两套运行时归档、Electron 原生模块重建、NSIS、post-build、provenance、sealed recovery、固定 Tag/target 与三项自动更新资产完整回下载全部通过。

## 正式发布

- 固定源码：`494d5c4d88ee356150a3a5dfa66e77d83ec6edec`；Tag 与 Release target 精确一致。
- 发布时间：`2026-08-20T04:53:29Z`；Release 非草稿、非预发布，并为 GitHub Latest。
- 安装包：`T8-PenguinCanvas-Setup-2.9.7.exe`，1,354,129,105 bytes，SHA-256 `e90973df761bb83a0006cff146433cea0ce929b6966457b9a4e274895f6dbc66`。
- blockmap：1,413,441 bytes，SHA-256 `aea0604cc63a05cc642c3db6a65fd9769594125e5a5cf2dbcbcf4838bac943c6`；`latest.yml`：362 bytes，SHA-256 `af306513d085c917a7bd78c38054ef08aa2627a14cd31eba0e6f4555319b28c2`。
- 正式流程完成 2,904 模块私有构建、157 个后端源与四个本机私有文件加密、两套运行时归档完整验证、Electron native rebuild、NSIS、post-build、provenance、sealed recovery、发布前三资产完整回下载及发布后 Latest/digest/size 核验；本地恢复记录已清除。

## 证据边界

用户授权先发布自动更新包、后补反馈用户安装、多设备协作、F8–F10 与安装升级证据，精确令牌为 `owner-approved-post-release-v2.9.7`。该延期不等于通过；任何技术构建、测试、摘要、固定 Tag/target、资产完整回下载或 Latest 门禁均不可延期。
