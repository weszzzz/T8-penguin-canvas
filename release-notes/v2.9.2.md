# T8 Penguin Canvas v2.9.2

本版集中优化 Electron 启动、画布目录、单画布加载、主题与媒体资源调度，并补齐跨画布后台运行的持久化安全边界。既有节点、模型、默认渠道、用户画布、工作流、Provider 协议以及 v2.9.1 和更早版本的全部能力保持不变。

## 启动与目录

- Electron 先显示可操作启动壳；主界面加载成功且 `ready-to-show` 后替换，15 秒有界兜底会显示重新加载画布和打开本地后端入口。
- 后端导入、`/api/status` 与前端壳不再同步打开项目数据库或协作 Gateway；数据库备份、语义刷新、运行恢复与 Figma Bridge 延后并受关机 fence 约束。
- 画布目录首屏按 50 项游标分页，侧栏采用窗口化渲染；损坏或缺失索引以小批次渐进恢复，请求热路径不回退到全库正文扫描。
- 保存后只定点刷新目标画布元数据；重命名先提交 SQLite 权威数据，兼容镜像故障不会把已提交结果误报为失败。

## 画布、主题与媒体

- 单画布只执行一次 canonical hydration；权威 GET 失败或超时时保持只读、显示重试且不会发送空画布 PUT，取得有效 revision 后才恢复写权限。
- 视口按画布写入本地 LRU；旧画布只在没有有效保存视口时执行一次 fit，快速切换前会冲刷待保存编辑。
- 主题 CSS 和大型主题体验按需加载并双缓冲切换；加载失败回退 tech，避免半主题与首屏外网字体阻塞。
- 图片、视频、音频共用单例可见性观察器和 4/1/1 并发调度；离屏资源不抢首屏，首次点击播放会保留用户意图。

## 跨画布后台运行安全

- 运行以 `canvasId + entityUid` 绑定原始画布实体，即使 A/B 画布出现相同可见 nodeId，也不会互相串写。
- 后台结果通过 revision CAS 的 `node.patch` 提交；冲突只重新读取并确认同一实体后有界重试，不用全量画布覆盖用户并发编辑。
- 已完成提交进入有界 mailbox，原画布重新挂载后按 revision 合并；节点已删除或同 ID 重建时不会被旧结果复活。
- 权威后台 patch 会使旧 autosave 代次失效；迟到的 409 不会污染已 rebase 的新 pending，也不会造成未处理 Promise 拒绝。

## 验证

- 受影响前端聚合矩阵 88/88、独立安全矩阵 59/59、后端/目录/生命周期矩阵 44/44 通过。
- TypeScript、生产构建、worktree、development、feature-sync、diff-check 通过；数据库方法分类 460/460 完整，该数字只证明分类清单完整，不代表全库 writer policy 已完全合规。
- 隔离开发态真实 Chrome、5000 项目录：冷导航到可交互 959.1ms、刷新 464.9ms，侧栏最多挂载 14 行；应用网络错误、HTTP 错误、console error、pageerror 与横向溢出均为 0。
- 上述性能数据来自本机开发态隔离环境，不代表 Electron 安装版、真实用户磁盘、杀软、VPN/TUN 或所有机器均达到同一数值。

## 范围与待补证据

- MiniMax Music3 实验代码及上游模板资源未纳入 v2.9.2：官方公开仓库未声明许可证元数据，本版不再分发未获许可的上游模板。
- v2.9.2 已于 `2026-08-14T18:34:43Z` 从固定源码 `94ce2523b6b486c86bcee63d19d68754e451cbd0` 发布为非草稿、非预发布的 GitHub Latest：[下载与发布说明](https://github.com/T8mars/T8-penguin-canvas/releases/tag/v2.9.2)。唯一正式流程完成私有 sidecar、后端加密、双运行时归档、原生模块重建、NSIS、post-build、provenance、sealed recovery、草稿所有权、发布前三资产完整回下载、固定 Tag/target、Latest 与 recovery 清理；独立发布后验证再次完整下载三项公开资产并通过实际字节与 GitHub digest 对账。
- 安装包 `T8-PenguinCanvas-Setup-2.9.2.exe` 为 `1,314,962,979` 字节，SHA-256 `2e37993266078f235dec0232c2a5ead98bfd829e86ef5a46a09f2cf926bd7724`；blockmap 为 `1,369,869` 字节，SHA-256 `757c12dc519ead259f43ddc1e6b75a43cd8df6dc4648e0fcb5733ea436c54c08`；`latest.yml` 为 `362` 字节，SHA-256 `d9e9effa5ddd17ba4de233e92530fbfd47591497e566c00cff9c4b7c86fe3b4c`。
- 既有 watermark runtime ZIP64 从同一 `47,734` 个源文件重建并由官方系统 7-Zip 24.06 完整 CRC；bundled 7-Zip 21.07 对有效 ZIP64 的错误拒绝未被当成通过，正式包中的归档与验证源逐字节一致。
- 用户已明确授权先发布、后补反馈用户安装环境、多人协作和安装升级证据；只允许精确 `owner-approved-post-release-v2.9.2` 对完全缺失的证据清单延期，延期不会标记为通过，任何技术门禁失败均不可延期。
